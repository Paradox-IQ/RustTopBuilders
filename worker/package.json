{
  "name": "rusttop-suggest-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that proxies builder-suggestion submissions to a Discord webhook with Turnstile + per-IP rate limit.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240924.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.78.0"
  }
}
